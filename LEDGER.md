# Experiment Ledger

Primary metric: official SSKit `mAP-LocSim` (higher is better).

| Date | Tag | Runtime | Command/Payload | Score | Threshold | Decision | Notes |
|---|---|---|---|---:|---:|---|---|
| 2026-04-26 | cloud-smoke | HF Jobs `t4-small` | `cloud/synloc_smoke.py` | 0.0000000000 | 0.000000 | keep | Verified GPU, SoccerNet, SSKit, and private HF write after packaging fixes. |
| 2026-04-26 | dataset-cache-valid | HF Jobs `cpu-upgrade` | `cloud/synloc_cache.py` | 0.0000000000 | 0.000000 | keep | Cached `fullhd` validation data in private HF dataset repo. |
| 2026-04-27 | baseline-probe | HF Jobs `t4-small` | `cloud/synloc_baseline_yolo.py` | 0.0001237624 | 0.499843 | keep | 64-image TorchVision Faster R-CNN bottom-center baseline; proves metric path, not a good model direction. |
| 2026-04-27 | baseline-full | HF Jobs `l4x1` | `cloud/synloc_baseline_yolo.py` | 0.0000356151 | 0.499957 | keep | Full 6,777-image validation baseline with generic COCO `person` detections. |
| 2026-04-29 | soccermaster-wiring-corrected | HF Jobs `t4-small` | `cloud/soccermaster_wiring_probe.py` | 0.0000000000 | 0.000000 | keep-as-evidence | Correct official role labels yielded `player=1196`, `referee=4`; prior zero-athlete result was a decode/config issue. |
| 2026-04-29 | soccermaster-synloc-eval-probe | HF Jobs `t4-small` | `cloud/soccermaster_synloc_eval_probe.py` | 0.0000073739 | 0.010000 | discard-as-baseline | Copied adapter converted detections but remained worse than generic 64-image baseline; not a source-faithful SoccerMaster verdict. |
| 2026-04-29 | pretrained-yolo-baseline | HF Jobs `t4-small` | `train.py TRAIN_MODE=baseline` | 0.0000467028 | 0.010000 | plumbing-audit | YOLO26l detections existed but official SynLoc recall was `0.0`; audit class ids, preprocessing, bbox format, projection, and evaluator ingestion before treating this as model underperformance. |
| 2026-04-29 | synloc-devkit-oracle | HF Jobs `cpu-upgrade` | `cloud/synloc_devkit_oracle.py` | 0.9809895759 | 0.500000 | keep | Exact GT scored `1.0`; SSKit-projected GT keypoint scored `0.9809895759`; bbox bottom-center scored `0.5686594909`. |
| 2026-05-01 | football-yolo26-diagnostic | HF Jobs `t4-small` | `train.py` diagnostic | 0.0000467028 | 0.474884 | discard-direction | Image-space GT recall stayed near zero: `gt_recall_iou_0_5=0.0005260389`. |
| 2026-05-02 | synloc-pose-smoke | HF Jobs `t4-small` | `cloud/synloc_pose_smoke.py` | 0.0008250825 | 0.360525 | keep-signal | Non-promotable validation-slice smoke, but much better than detector-only rows and points toward pose/keypoint. |
| 2026-05-04 | dataset-cache-train-valid | HF Jobs `cpu-upgrade` | `cloud/synloc_cache.py` | 0.0000000000 | 0.000000 | keep | Cached train/valid data for the next source-specific experiment. |
| 2026-05-04 | first-yolo-train | HF Jobs `t4-small` | `train.py TRAIN_MODE=finetune` | 0.0000035728 | 0.478651 | discard | `recall_50=0.0`, worse than pose smoke `0.000825082508250825`. Do not run another generic detector fine-tune without a new representation. |
| 2026-05-04 | keypoint-yolo11n-smoke | HF Jobs `t4-small` | `train.py TRAIN_MODE=keypoint` | 0.0000005743 | 0.203447 | discard-result | One-epoch YOLO11n-pose footpoint smoke on 128 train / 64 valid images completed via job `69f8ea6e98a8d679adfb90a5`. Official `position_from_keypoint_index=0` path worked, but `recall_50=0.0`, `gt_recall_px_50=0.0518`, and 14,912 noisy detections made it worse than the pose smoke. Keep the keypoint wiring; do not scale this exact config. |
| 2026-05-04 | keypoint-topk25-smoke | HF Jobs `t4-small` | `train.py TRAIN_MODE=keypoint YOLO_MAX_DETECTIONS_PER_IMAGE=25` | 0.0000000000 | 0.229000 | plumbing-audit | Job `69f92f9898a8d679adfb9328` trained one epoch on 128 train / 64 valid images and capped validation predictions to top 25 per frame. Official SSKit ran and printed `mAP-LocSim=0.000`, `precision_50=0.000`, `recall_50=0.000`, `frame_accuracy=0.000`; then artifact upload failed with HF model-repo LFS `403` because the connector token had read but not write permission. Treat as a prediction/eval plumbing warning, not a model verdict. |
| 2026-05-05 | keypoint-audit-samples | Local edit + blocked HF submit | `train.py TRAIN_MODE=keypoint SYNLOC_AUDIT_SAMPLE_IMAGES=5` | 0.0000000000 | 0.000000 | keep-mechanics / cloud-blocked | Added validation audit examples with model class names, raw class counts, GT bbox/keypoint rows, top predicted keypoints, component scores, and nearest-GT pixel distance. Tiny `t4-small` smoke submission was attempted but failed before job creation because local `hf` has no token/login, so there is no official score yet. |
| 2026-05-05 | keypoint-score-mode-audit | Local edit + blocked HF submit | `train.py TRAIN_MODE=keypoint YOLO_KEYPOINT_SCORE_MODE={combined,box,keypoint}` | 0.0000000000 | 0.000000 | keep-mechanics / cloud-blocked | Added a bounded keypoint candidate scoring switch so the next tiny HF smoke can test whether official `mAP-LocSim` improves when top-k filtering and COCO scores use box confidence, keypoint confidence, or their product. Local shell is still not logged into HF, so no job or official score was created in this pass. |
| 2026-05-05 | keypoint-score-mode-matrix | Local edit + blocked HF submit | `train.py TRAIN_MODE=keypoint YOLO_KEYPOINT_SCORE_MODE=matrix` | 0.0000000000 | 0.000000 | keep-mechanics / cloud-blocked | Added a one-job score-mode matrix: train the tiny keypoint smoke once, then evaluate the same checkpoint with `combined`, `box`, and `keypoint` ranking and promote the best official `mAP-LocSim` inside `summary.json`. Tried `hf auth whoami`; the shell is still not logged in, so no HF job or official score was created. |
| 2026-05-05 | keypoint-score-mode-matrix-run | HF Jobs `t4-small` | `train.py TRAIN_MODE=keypoint YOLO_KEYPOINT_SCORE_MODE=matrix` | 0.0000074072 | 0.198693 | discard-result | Job `69f938fd98a8d679adfb9346` trained one epoch on 64 train / 32 valid images and evaluated `combined`, `box`, and `keypoint` ranking from the same checkpoint. Best was `combined`; `box=0.0000070553`, `keypoint=0.0`, all with `recall_50=0.0`. Diagnostics stayed weak: `gt_recall_px_50=0.0171`, `gt_recall_iou_0_5=0.0019`, so confidence-only ranking is not the next lever. |
| 2026-05-05 | bbox-bottom-center-keypoint-target | Local edit + blocked HF submit | `train.py TRAIN_MODE=keypoint SYNLOC_KEYPOINT_TARGET=bbox_bottom_center` | 0.0000000000 | 0.000000 | keep-mechanics / cloud-blocked | Added a keypoint target switch so the pose lane can train/evaluate either the original annotation keypoint or GT bbox bottom-center. Expected movement is positive because oracle bbox bottom-center through SSKit scored `0.5686594909`, and this is an easier image-space target than the projected ground keypoint. Tried a tiny `t4-small` smoke with 64 train / 32 valid / 1 epoch, but `hf` failed before job creation with `LocalTokenNotFoundError`; local shell has no HF login/token. |
| 2026-05-05 | bbox-bottom-center-keypoint-smoke | HF Jobs `t4-small` | `train.py TRAIN_MODE=keypoint SYNLOC_KEYPOINT_TARGET=bbox_bottom_center` | 0.0000073160 | 0.198697 | discard-result | Job `69f93dc79d85bec4d76f29ed` trained one epoch on 64 train / 32 valid images using the GT bbox bottom-center proxy as the single keypoint target. Official `mAP-LocSim=7.316002536214212e-06`, `precision_50=0.0024630541871921183`, `recall_50=0.0`, `gt_recall_px_50=0.017110266159695818`, and `gt_recall_iou_0_5=0.0019011406844106464`; this matches the weak score-mode matrix and stays far below pose smoke, so target switching alone does not rescue the YOLO11n-pose lane. |
| 2026-05-05 | direct-point-regressor-oracle-candidates | Local edit + blocked HF submit | `train.py TRAIN_MODE=point_regressor` | 0.0000000000 | 0.000000 | keep-mechanics / cloud-blocked | Added a tiny direct crop point-regressor lane that uses GT boxes as oracle validation candidates and evaluates predicted points through official SSKit `position_from_keypoint_index=0`. This isolates point quality from candidate recall and is not challenge-submittable. Prepared the tiny smoke command for 64 train / 32 valid / 2 epochs, but `hf auth whoami` returned `Not logged in`, so no HF job or official score was created. |
| 2026-05-05 | point-regressor-crop-bounds-fix | HF Jobs `t4-small` + local edit | `TRAIN_MODE=point_regressor ... POINT_EPOCHS=2` | 0.0000000000 | 0.000000 | keep-mechanics | First connector job `69fa5a17f2f4addb7839bfb4` failed before scoring with `ValueError: Coordinate 'right' is less than 'left'` in `PointCropDataset`; patched `crop_bounds()` so finite boxes always produce an in-image positive-area crop and added invariant coverage to `scripts/verify.sh`. |
| 2026-05-05 | direct-point-regressor-oracle-smoke | HF Jobs `t4-small` | `TRAIN_MODE=point_regressor TRAIN_MAX_IMAGES=64 VAL_MAX_IMAGES=32 POINT_EPOCHS=2 POINT_BATCH=16` | 0.0027874658 | 0.500000 | keep-signal / not-submittable | Job `69fa5b70f2f4addb7839bfba` completed official SSKit eval using GT boxes as oracle candidates: `precision_50=1.0`, `recall_50=0.0`, `gt_recall_iou_0_5=1.0`, `gt_recall_px_50=0.39272030651340994`, epoch losses `[0.03522297692653097, 0.01844084079636902]`. This beats pose smoke `0.000825082508250825`, so direct point regression has signal, but the setup is not challenge-submittable until paired with real candidates. Artifact upload failed with HF model-repo LFS `403` read-only token. |
| 2026-05-05 | point-regressor-yolo-candidates | HF Jobs `t4-small` | `TRAIN_MODE=point_regressor POINT_CANDIDATE_MODE=yolo TRAIN_MAX_IMAGES=64 VAL_MAX_IMAGES=32 POINT_EPOCHS=2 POINT_BATCH=16` | 0.0000000000 | 0.473680 | discard-result | Job `69fa5f40f2f4addb7839bfd9` trained the same tiny crop point regressor, then evaluated it on `mobadam/football-player-detection` YOLO26 candidate boxes with top-25 per image. Official `mAP-LocSim=0.0`, `precision_50=0.0`, `recall_50=0.0`; diagnostics show candidate generation failed before point quality mattered: `gt_recall_iou_0_5=0.0`, `gt_recall_iou_0_3=0.0019011406844106464`, `gt_recall_px_50=0.0019011406844106464`, 508 predictions for 526 GT boxes. Artifact upload again failed with HF model-repo LFS `403`. |
| 2026-05-05 | yolo11n-coco-person-candidate-audit | HF Jobs `t4-small` | `TRAIN_MODE=baseline YOLO_BASELINES=yolo11n-coco-person\|ultralytics\|yolo11n.pt\|0 TRAIN_MAX_IMAGES=64` | 0.0000405501 | 0.422596 | discard-result | Job `69fa6402f2f4addb7839bffe` evaluated public Ultralytics `yolo11n.pt` COCO person boxes through official SSKit bottom-center projection. It produced 4,014 person detections for 1,004 GT boxes but still had `recall_50=0.0`, `gt_recall_iou_0_5=0.00099601593625498`, and `mean_best_iou_gt_to_det=0.0054024969727797115`, so generic COCO person candidates do not unblock the direct point regressor. Earlier launches `69fa629af2f4addb7839bff6`, `69fa62ddb745af80fb373474`, and `69fa6398b745af80fb373478` were packaging retries for `xtcocotools`/NumPy; working recipe pinned `numpy<2`. Artifact upload still failed with HF model-repo `403`. |
| 2026-05-05 | football-yolo26-imgsz1600-candidate-audit | HF Jobs `t4-small` | `TRAIN_MODE=baseline YOLO_BASELINES=football-yolo26l-imgsz1600\|mobadam/football-player-detection\|player_detector.pt\|1,3 TRAIN_MAX_IMAGES=64 YOLO_IMGSZ=1600 YOLO_CONF=0.001 YOLO_IOU=0.7` | 0.0000048773 | 0.477361 | discard-result | Job `69fa6701f2f4addb7839c00a` tested whether the football YOLO26 candidate failure was an under-resolved or over-thresholded inference issue. It produced 2,125 detections for 1,004 GT boxes, but official `recall_50=0.0`, `gt_recall_iou_0_5=0.0`, `gt_recall_iou_0_3=0.00099601593625498`, and `mean_best_iou_gt_to_det=0.0012062984910266583`. Larger image size and lower confidence did not rescue this candidate source. Artifact upload still failed with HF model-repo `403`. |
| 2026-05-05 | easychamp-martinjolif-source-candidate-audit | HF Jobs `t4-small` | `TRAIN_MODE=baseline YOLO_BASELINES=easychamp-yolov8\|aabyzov/easychamp-player-detection-yolov8\|player_detection_best.pt\|0,2,3;martinjolif-yolo11m\|martinjolif/yolo-football-player-detection\|yolo-football-player-detection.pt\|0,1,2 TRAIN_MAX_IMAGES=64 YOLO_IMGSZ=1280 YOLO_CONF=0.01` | 0.0000108208 | 0.459440 | discard-result | Job `69fa6a3ab745af80fb3734f9` evaluated two different soccer-specific public detector candidates on the same 64 validation frames. Best was `martinjolif-yolo11m` with 1,120 detections for 1,004 GT boxes, but official `recall_50=0.0`, `gt_recall_iou_0_5=0.0`, `gt_recall_iou_0_3=0.00099601593625498`, and `mean_best_iou_gt_to_det=0.0013935321462024005`; `easychamp-yolov8` was similar at `mAP-LocSim=9.32798528843463e-06`. First connector launch `69fa6a1db745af80fb3734f7` failed before scoring on the known Python 3.12 `xtcocotools` build-isolation issue; Python 3.10 fixed packaging. Artifact upload again failed with HF model-repo `403`. |
| 2026-05-05 | rtdetr-r18-coco-person-candidate-audit | HF Jobs `t4-small` | `TRAIN_MODE=transformer_baseline TRANSFORMER_BASELINES=rtdetr-r18-coco-person\|PekingU/rtdetr_r18vd\|0 TRAIN_MAX_IMAGES=64 TRANSFORMER_CONF=0.05` | 0.0000075494 | 0.446121 | discard-result | Job `69fa6d7ff2f4addb7839c046` evaluated a non-YOLO COCO RT-DETR person detector on the same 64 validation-frame slice through official SSKit bottom-center projection. It produced 10,006 detections for 1,004 GT boxes, but official `recall_50=0.0`; diagnostics were better than the YOLO candidate audits but still weak: `gt_recall_iou_0_5=0.0069721115537848604`, `gt_recall_iou_0_3=0.0348605577689243`, and `mean_best_iou_gt_to_det=0.029056858644601863`. Keep the reusable `TRAIN_MODE=transformer_baseline` mechanics, but discard COCO RT-DETR as a candidate source for the point regressor. Artifact upload still failed with HF model-repo `403`. |
| 2026-05-05 | uisikdag-yolov8-football-candidate-audit | HF Jobs `t4-small` | `TRAIN_MODE=baseline YOLO_BASELINES=uisikdag-yolov8-football-athletes\|uisikdag/yolo-v8-football-players-detection\|best.pt\|1,2,3 TRAIN_MAX_IMAGES=64 YOLO_IMGSZ=1280 YOLO_CONF=0.01` | 0.0000070848 | 0.468737 | discard-result | Corrected job `69fa71b6b745af80fb373541` evaluated the older public Uisikdag football YOLOv8 source on the same 64 validation frames after initial job `69fa7058f2f4addb7839c058` exposed a class-id plumbing mistake by selecting `ball=0`; model labels are `0=ball`, `1=goalkeeper`, `2=player`, `3=referee`. The corrected athlete run produced 2,265 detections for 1,004 GT boxes, but official `recall_50=0.0`, `gt_recall_iou_0_5=0.0`, `gt_recall_iou_0_3=0.00298804780876494`, and `mean_best_iou_gt_to_det=0.003439228441225626`, so this public football YOLO source does not unblock direct point regression. Artifact upload still failed with HF model-repo `403`. |
| 2026-05-05 | rfdetr-soccernet-runtime-parity | HF Jobs `t4-small` | `TRAIN_MODE=rfdetr_baseline TRAIN_MAX_IMAGES=16 RFDETR_CONF=0.5` | 0.0000000000 | 0.000000 | integration-blocked | Added a bounded `TRAIN_MODE=rfdetr_baseline` lane for `julianzu9612/RFDETR-Soccernet`, a SoccerNet-Tracking RF-DETR checkpoint with `ball/player/referee/goalkeeper` labels. Jobs `69fa7566b745af80fb373571` and `69fa76dbb745af80fb373583` failed with checkpoint shape mismatches under current `rfdetr`; pinning `rfdetr==1.2.1` and `transformers==4.50.0` in job `69fa794bf2f4addb7839c074` fixed the import break but still failed before scoring because checkpoint tensors such as backbone projector weights are `192/384` wide while `RFDETRBase` expects `128/256`. Treat this as an official-runtime parity blocker, not a model verdict. Next RF-DETR step must identify the exact architecture/class or `rfdetr_plus`/legacy runtime before spending another 15 GB download on scoring. |
| 2026-05-05 | rfdetr-soccernet-large-parity-smoke | HF Jobs `t4-small` | `TRAIN_MODE=rfdetr_baseline TRAIN_MAX_IMAGES=16 RFDETR_MODEL_CLASS=RFDETRLarge RFDETR_CONF=0.5` | 0.0000000000 | 0.454406 | discard-result / keep-mechanics | Job `69fa7cefb745af80fb3735c0` loaded `julianzu9612/RFDETR-Soccernet` with `RFDETRLarge`, resolving the prior base-vs-large tensor mismatch and allowing official SSKit scoring. The 16-frame smoke produced 156 detections for 301 GT boxes but official `precision_50=0.0`, `recall_50=0.0`, `gt_recall_iou_0_5=0.0`, and `mean_best_iou_gt_to_det=0.0012578130198192673`. Keep the `RFDETRLarge` loader mechanics, but discard this checkpoint as a candidate source unless a new preprocessing/coordinate-parity hypothesis appears. Artifact upload still failed with HF model-repo `403`. |
| 2026-05-05 | soccana-yolo11-soccernet-candidate-audit | HF Jobs `t4-small` | `TRAIN_MODE=baseline YOLO_BASELINES=soccana-yolo11-soccernet\|Adit-jain/soccana\|Model/weights/best.pt\|0,1,2,3 TRAIN_MAX_IMAGES=64 YOLO_IMGSZ=1280 YOLO_CONF=0.01` | 0.0000430478 | 0.425670 | discard-result | Job `69fa8013b745af80fb3735e4` evaluated `Adit-jain/soccana`, a YOLO11 soccer detector whose labels loaded as `0=Player`, `1=Ball`, `2=Referee`, on the same 64 validation-frame candidate audit path. It produced 1,584 detections for 1,004 GT boxes, but official `recall_50=0.0`, `gt_recall_iou_0_5=0.0`, `gt_recall_iou_0_3=0.00199203187250996`, and `mean_best_iou_gt_to_det=0.0018615731134729054`, so this SoccerNet-labeled public detector does not unblock direct point regression. First wrapper job `69fa7ffef2f4addb7839c083` failed before scoring because wrapper dependencies omitted `numpy`; artifact upload still failed with HF model-repo `403`. |
| 2026-05-05 | point-regressor-jittered-candidates | HF Jobs `t4-small` | `TRAIN_MODE=point_regressor POINT_CANDIDATE_MODE=jittered POINT_JITTER_CENTER_FRAC=0.10 POINT_JITTER_SCALE_FRAC=0.15 TRAIN_MAX_IMAGES=64 VAL_MAX_IMAGES=32 POINT_EPOCHS=2 POINT_BATCH=16` | 0.0006912422 | 0.500000 | discard-result / blocker-found | Job `69fabd24b745af80fb3738a3` reran the same bounded smoke after job `69fabbf2b745af80fb373885` printed the score but hid it behind the known HF model-repo LFS `403` traceback. The score is below pose smoke `0.000825082508250825` and far below oracle GT-box point regression `0.0027874657923080423`. Diagnostics: `gt_recall_iou_0_5=0.3563218390804598`, `gt_recall_px_50=0.39272030651340994`, 522 predictions for 522 GT boxes. Audit examples exposed an image/annotation scale blocker: several GT boxes have x coordinates like `2442`, `2737`, and `2928`, but the opened image width forced jittered boxes to clamp at `1919`, producing zero-IoU candidates. Next pass should audit image file choice and coordinate scale before spending more candidate-source compute. Artifact upload again failed with HF model-repo LFS `403`; the `AUTONOMY_RESULT` recap in logs is the durable score source. |
| 2026-05-05 | image-path-dimension-guard | Local edit | `python3 -m py_compile train.py`; `scripts/verify.sh` | 0.0000000000 | 0.000000 | keep-mechanics / blocker-update | Added dimension-aware SynLoc image lookup in `train.py`: callers with COCO image records now require the resolved PIL image size to match annotation `width`/`height`, and fail loudly with candidate sizes instead of silently selecting a resized same-basename file. `scripts/verify.sh` now covers the duplicate-basename resized/fullhd bug family. No cloud score was launched from this dirty mechanics pass; next loop should rerun one cheap official smoke, ideally the jittered GT candidate audit, to see whether candidate IoU recovers. |
| 2026-05-05 | image-cache-scale-blocker-cloud | HF Jobs `t4-small` | `TRAIN_MODE=point_regressor POINT_CANDIDATE_MODE=jittered POINT_JITTER_CENTER_FRAC=0.10 POINT_JITTER_SCALE_FRAC=0.15 TRAIN_MAX_IMAGES=64 VAL_MAX_IMAGES=32 POINT_EPOCHS=2 POINT_BATCH=16` | 0.0000000000 | 0.000000 | blocker-update | Job `69fac036f2f4addb7839c18b` reran the jittered-GT smoke through committed `train.py` after the dimension guard. It failed before training/evaluation with `No image candidate for '000000.jpg' matched annotation size 3840x2160; candidates: /tmp/SoccerNet/train/000000.jpg=1920x1080, /tmp/SoccerNet/val/000000.jpg=1920x1080`. This proves the cached `fullhd` annotations are being paired with 1920x1080 image files in the cloud data path; fix cache extraction or add a deliberate coordinate-scale adapter before more detector/model searches. First connector job `69fabfe7f2f4addb7839c185` failed before running on Python 3.12 due to the known `xtcocotools`/`numpy` build-isolation issue; Python 3.10 remains the working job recipe. |
| 2026-05-06 | actual-image-scale-jittered-point-smoke | HF Jobs `t4-small` | `TRAIN_MODE=point_regressor SYNLOC_COORD_SCALE_MODE=actual_image POINT_CANDIDATE_MODE=jittered POINT_JITTER_CENTER_FRAC=0.10 POINT_JITTER_SCALE_FRAC=0.15 TRAIN_MAX_IMAGES=64 VAL_MAX_IMAGES=32 POINT_EPOCHS=2 POINT_BATCH=16` | 0.0032123790 | 0.500000 | keep-signal / not-submittable | Job `69fac2a1b745af80fb3738f9` reran the jittered-GT direct point-regressor smoke with an explicit coordinate-scale adapter: train/crop on actual 1920x1080 images, then emit prediction boxes/keypoints back in 3840x2160 annotation coordinates for SSKit. This recovered the scale-contaminated candidate diagnostics: `gt_recall_iou_0_5=0.9578544061302682`, `mean_best_iou_gt_to_det=0.6846319161542074`, `gt_recall_px_50=0.9980842911877394`, and `mean_best_px_gt_to_pred=12.602958457397472`. Official `mAP-LocSim=0.0032123790168145185`, above the previous jittered smoke `0.0006912422136296282`, pose smoke `0.000825082508250825`, and GT-box oracle direct point smoke `0.0027874657923080423`, so keep the adapter. This is still not challenge-submittable because candidate boxes are jittered GT. Artifact upload again failed with HF model-repo LFS `403`; the printed `AUTONOMY_RESULT` log is the durable result. First connector launch `69fac28cf2f4addb7839c1a5` failed before running because the raw GitHub URL had a bad SHA and returned `404: Not Found`. |
| 2026-05-06 | actual-image-scale-yolo26-point-smoke | HF Jobs `t4-small` | `TRAIN_MODE=point_regressor SYNLOC_COORD_SCALE_MODE=actual_image POINT_CANDIDATE_MODE=yolo TRAIN_MAX_IMAGES=64 VAL_MAX_IMAGES=32 POINT_EPOCHS=2 POINT_BATCH=16` | 0.0000079847 | 0.473680 | discard-result | Job `69fac472b745af80fb37390f` reran the direct point regressor with the scale adapter and real default `mobadam/football-player-detection` YOLO26 candidate boxes. Official `mAP-LocSim=7.984669434685405e-06`, `precision_50=0.002688172043010753`, `recall_50=0.0`, with 508 predictions for 526 GT boxes. Candidate diagnostics improved over the pre-adapter YOLO bridge but are still far too weak: `gt_recall_iou_0_5=0.0019011406844106464`, `gt_recall_iou_0_3=0.009505703422053232`, `mean_best_iou_gt_to_det=0.0072488041644957375`, and `gt_recall_px_50=0.039923954372623575`. The scale adapter did not rescue this real candidate source. Artifact upload again failed with HF model-repo LFS `403`; the printed `AUTONOMY_RESULT` log is the durable result. |
| 2026-05-06 | detector-baseline-scale-adapter | Local edit | `python3 -m py_compile train.py`; `scripts/verify.sh` | 0.0000000000 | 0.000000 | keep-mechanics | Extended `SYNLOC_COORD_SCALE_MODE=actual_image` beyond the point-regressor YOLO bridge into the YOLO, transformer, and RF-DETR detector-audit lanes. Detector boxes produced on cached 1920x1080 images are now mapped back to 3840x2160 annotation coordinates before image-space diagnostics, bottom-center projection, and official SSKit evaluation. `scripts/verify.sh` now covers the actual-image detector-box backscale invariant. No official score was launched from the dirty mechanics pass; next loop can run a tiny actual-image candidate audit without repeating the scale-contaminated baseline path. |
| 2026-05-06 | actual-image-hamza-football-candidate-audit | HF Jobs `t4-small` | `TRAIN_MODE=baseline SYNLOC_COORD_SCALE_MODE=actual_image YOLO_BASELINES=hamza-football-players\|HamzaAliKhan/football-players-detection\|best_players_detection.pt\|0,1,2,3 TRAIN_MAX_IMAGES=64 YOLO_IMGSZ=1280 YOLO_CONF=0.01` | 0.0000134077 | 0.450216 | discard-result | Job `69fac706b745af80fb373923` evaluated an untried public football detector through the repaired actual-image detector-audit path. Labels loaded as `0=ball`, `1=goalkeeper`, `2=player`, `3=referee`; 2,290 detections for 1,004 GT boxes still yielded official `recall_50=0.0`, `precision_50=0.002554278416347382`, `gt_recall_iou_0_5=0.00099601593625498`, `gt_recall_iou_0_3=0.012948207171314742`, and `mean_best_iou_gt_to_det=0.00950942234585973`. This is below pose smoke and does not unblock the direct point regressor. Artifact upload again failed with HF model-repo `403`; the printed `AUTONOMY_RESULT` log is the durable score. First connector launch `69fac6f1f2f4addb7839c1af` failed before running because the raw GitHub URL used a mistyped SHA and returned `404: Not Found`. |
| 2026-05-06 | actual-image-rtdetr-coco-person-candidate-audit | HF Jobs `t4-small` | `TRAIN_MODE=transformer_baseline SYNLOC_COORD_SCALE_MODE=actual_image TRANSFORMER_BASELINES=rtdetr-r18-coco-person-actual-image\|PekingU/rtdetr_r18vd\|0 TRAIN_MAX_IMAGES=64 TRANSFORMER_CONF=0.05` | 0.0000075436 | 0.446105 | discard-result | Job `69fac8c4b745af80fb373937` reran the strongest prior non-YOLO public candidate through the repaired actual-image transformer-audit path. It produced 10,000 COCO person detections for 1,004 GT boxes, but official `recall_50=0.0`, `precision_50=0.0013333333333333333`, `gt_recall_iou_0_5=0.0069721115537848604`, and `mean_best_iou_gt_to_det=0.02903191841477167`. This is effectively unchanged from the pre-adapter RT-DETR audit, so coordinate backscaling does not rescue generic RT-DETR person candidates. Artifact upload again failed with HF model-repo `403`; the printed `AUTONOMY_RESULT` log is the durable score. |
| 2026-05-06 | repo-local-hf-helper-env-miss | Local helper diagnosis | `hf auth whoami`; `scripts/run_hf_train.sh --dry-run` | 0.0000000000 | 0.000000 | helper-bug | This loop exposed a repo-local helper problem, later corrected by `hf-env-loader-correction`: `scripts/run_hf_train.sh` checked only inherited shell state and did not source the repo-local `.env` that already contains `HF_TOKEN`. The helper could construct a committed, repo-relative Python 3.10 `t4-small` command against `41b84dd583cffa3effb5508251aaed2990dde3e8`, so the broken surface was helper environment loading, not Hugging Face access. No model score was attempted from this helper check. |
| 2026-05-06 | hf-dry-run-reachability-guard | Local helper fix | `HF_GIT_REF=definitely-not-a-real-ref scripts/run_hf_train.sh --dry-run` | 0.0000000000 | 0.000000 | keep-mechanics / blocker-update | Tightened the repo-local HF helper so dry-run now checks that the selected git ref exists on the configured remote before printing a raw GitHub `train.py` URL. This blocks the bad-SHA/404 job-launch failure family before an operator copies a dry-run command. This is a packaging guard, not a credential verdict. |
| 2026-05-06 | hf-submit-preflight | Local helper fix | `hf auth whoami`; `scripts/run_hf_train.sh --preflight`; `bash -n scripts/run_hf_train.sh`; `scripts/verify.sh` | 0.0000000000 | 0.000000 | keep-mechanics / superseded-diagnosis | Added a non-submitting preflight mode to the repo-local HF helper so the next loop can verify `hf` availability, visible credentials, clean committed code, and reachable `train.py` before launching a private SynLoc job. The initial preflight failure was later traced to the helper not sourcing `.env`, not to real missing HF access. No cloud score was attempted from this helper check. |
| 2026-05-06 | hf-env-loader-correction | Local helper fix | `source .env; hf auth whoami`; `scripts/run_hf_train.sh --preflight`; `scripts/verify.sh` | 0.0000000000 | 0.000000 | keep-mechanics / correction | Corrected the previous repo-local HF diagnosis: the helper was not loading `.env`, so it falsely treated an unexported shell token as missing HF access. Sourcing `.env` authenticates as `dmontgomery40`; after the fix, `scripts/run_hf_train.sh` loads repo-local `.env` before building defaults or checking credentials, preflight reaches the pushed-ref command path, and dry-run output still does not leak `HF_TOKEN`. The earlier connector-launched HF Jobs were real; the broken path was only the repo-local helper's environment loading. |
| 2026-05-06 | hf-job-write-permission-blocker | Local submit attempt | `TRAIN_MODE=rfdetr_baseline SYNLOC_COORD_SCALE_MODE=actual_image TRAIN_MAX_IMAGES=32 RFDETR_MODEL_CLASS=RFDETRLarge RFDETR_CONF=0.1 scripts/run_hf_train.sh` | 0.0000000000 | 0.000000 | cloud-blocked | Chose a bounded SoccerNet RF-DETR Large actual-image candidate audit because the prior RF-DETR Large smoke scored before the detector lanes had coordinate backscaling. `scripts/run_hf_train.sh --preflight` passed against committed ref `489ea13187588166f6d410cf526d36981197b3fc`, but actual submission failed before job creation with HF `403` on `https://huggingface.co/api/jobs/dmontgomery40`: token is authenticated but lacks `job.write` for namespace `dmontgomery40`. No official score exists. Do not relabel this as a model result; rerun this exact bounded command after the repo-local token has Jobs write permission. |
| 2026-05-06 | hf-job-write-permission-recheck | Local submit attempt | `TRAIN_MODE=rfdetr_baseline SYNLOC_COORD_SCALE_MODE=actual_image TRAIN_MAX_IMAGES=32 RFDETR_MODEL_CLASS=RFDETRLarge RFDETR_CONF=0.1 scripts/run_hf_train.sh -- --env SYNLOC_COORD_SCALE_MODE=actual_image --env TRAIN_MAX_IMAGES=32 --env RFDETR_MODEL_CLASS=RFDETRLarge --env RFDETR_CONF=0.1` | 0.0000000000 | 0.000000 | cloud-blocked | Rechecked the same selected RF-DETR Large actual-image audit from pushed ref `092a50188d92eb6ebb1000e0e89ff929d84b5972`. Helper preflight produced a reachable repo-relative raw GitHub command, but actual HF Jobs creation again failed before job id or scoring with HF `403`: missing `job.write` for namespace `dmontgomery40`. No model verdict exists; the next loop should not spend model-reasoning time until the token scope/namespace permission is fixed. |
| 2026-05-06 | actual-image-rfdetr-soccernet-large-candidate-audit | HF Jobs connector `t4-small` | `TRAIN_MODE=rfdetr_baseline SYNLOC_COORD_SCALE_MODE=actual_image TRAIN_MAX_IMAGES=32 RFDETR_MODEL_CLASS=RFDETRLarge RFDETR_CONF=0.1` | 0.0000000000 | 0.454406 | discard-result | Job `69fb0327b745af80fb373bd5` used the Hugging Face Jobs connector to run the exact RF-DETR Large audit that the local `hf` CLI token could not submit. It downloaded SynLoc data and `julianzu9612/RFDETR-Soccernet`, loaded `RFDETRLarge` on CUDA, and reached official SSKit eval. Result: 566 detections for 526 GT boxes, official `mAP-LocSim=0.0`, `precision_50=0.0`, `recall_50=0.0`, `gt_recall_iou_0_5=0.0019011406844106464`, `gt_recall_iou_0_3=0.0076045627376425855`, and `mean_best_iou_gt_to_det=0.007630024549978594`. Discard this RF-DETR SoccerNet checkpoint as a candidate source under the current bottom-center projection path. Artifact upload failed after `AUTONOMY_RESULT` with HF model-repo direct-commit `403`; the printed log is the durable score. |
| 2026-05-06 | hf-jobwrite-and-upload-guard | Local helper fix | `scripts/run_hf_train.sh --preflight`; `scripts/verify.sh` | 0.0000000000 | 0.000000 | keep-mechanics | Tightened the repo-local loop mechanics exposed by job `69fb0327b745af80fb373bd5`: `scripts/run_hf_train.sh --preflight` now inspects fine-grained `HF_TOKEN` scopes and fails early with a clear `job.write` message instead of printing a command that will 403 at job creation; `program.md` and the Codex tick prompt route missing-`job.write` cases to the Hugging Face Jobs connector/app when available and otherwise record one blocker instead of repeating no-score passes. `train.py` now retries artifact upload as a Hub PR when direct commit is blocked and lets scored experiments exit successfully by default after printing `UPLOAD_RESULT_FAILED`; set `HF_STRICT_UPLOAD=1` only when upload failure should fail the job. |
| 2026-05-06 | point-regressor-train-jitter-smoke | HF Jobs connector `t4-small` | `TRAIN_MODE=point_regressor SYNLOC_COORD_SCALE_MODE=actual_image POINT_CANDIDATE_MODE=jittered POINT_JITTER_CENTER_FRAC=0.10 POINT_JITTER_SCALE_FRAC=0.15 POINT_TRAIN_JITTER_CENTER_FRAC=0.10 POINT_TRAIN_JITTER_SCALE_FRAC=0.15 TRAIN_MAX_IMAGES=64 VAL_MAX_IMAGES=32 POINT_EPOCHS=2 POINT_BATCH=16` | 0.0030130723 | 0.500000 | discard-result / code-reverted | Job `69fbcd06317220dbbd1a5657` tested whether training the direct point regressor on deterministic jittered GT crops improves robustness to jittered GT validation candidates. It reached official SSKit eval and scored below the previous no-train-jitter actual-image jittered smoke `0.0032123790168145185`, with `gt_recall_iou_0_5=0.9578544061302682`, `gt_recall_px_50=0.9980842911877394`, and mean point error about `12.67` px. The temporary `POINT_TRAIN_JITTER_*` code was reverted because the score did not justify the extra train surface. Artifact upload again failed after `AUTONOMY_RESULT` with HF model-repo LFS `403`; the printed log is the durable score. |
| 2026-05-06 | pericles-player-detector-candidate-audit | HF Jobs connector `t4-small` | `TRAIN_MODE=baseline SYNLOC_COORD_SCALE_MODE=actual_image YOLO_BASELINES=pericles-player-detector\|PericlesRodrigues01/player-detector\|best.pt\|0 TRAIN_MAX_IMAGES=64 YOLO_IMGSZ=1280 YOLO_CONF=0.01` | 0.0000840650 | 0.478658 | discard-result | Job `69fbd098aff1cd33e8f2eb52` evaluated the untried public `PericlesRodrigues01/player-detector` YOLOv8n sports detector through the repaired actual-image detector-audit path. Its README declares `0=Person`, `1=Ball`, `2=Equipment`, and the loaded model agreed. It produced 7,726 detections for 1,004 GT boxes and reached official SSKit eval, but candidate overlap stayed too weak for direct point regression: `recall_50=0.0`, `precision_50=0.009433962264150943`, `gt_recall_iou_0_5=0.00298804780876494`, `gt_recall_iou_0_3=0.013944223107569721`, and `mean_best_iou_gt_to_det=0.023235810243179132`. This is below the pose smoke `0.000825082508250825`, so discard this source. Artifact upload again failed after `AUTONOMY_RESULT` with HF model-repo `403`; the printed log is the durable score. |
| 2026-05-06 | split-specific-synloc-fetch | Local helper fix | `python3 -m py_compile train.py`; `scripts/verify.sh` | 0.0000000000 | 0.000000 | keep-mechanics | Pass 2 exposed a mechanics waste: validation-only detector audits were still asking Hugging Face for `raw/fullhd/*.zip`, so job `69fbd098aff1cd33e8f2eb52` downloaded the 12.8GB train archive before a 64-frame validation eval. Added `synloc_snapshot_patterns()` so baseline, transformer, and RF-DETR audits request only annotations, manifest, and the selected split archive; train/keypoint/point-regressor modes still request train plus valid. `scripts/verify.sh` now covers valid, train+valid alias dedupe, challenge patterns, and forbids run modes from using the wildcard fetch again. |
| 2026-05-06 | keremberke-yolov5m-runtime-blocker | HF Jobs connector `t4-small` | `TRAIN_MODE=baseline SYNLOC_COORD_SCALE_MODE=actual_image YOLO_BASELINES=keremberke-yolov5m-football\|keremberke/yolov5m-football\|best.pt\|0,1,2,3 TRAIN_MAX_IMAGES=16 YOLO_IMGSZ=1280 YOLO_CONF=0.01` | 0.0000000000 | 0.000000 | integration-blocked | Job `69fbd3a2317220dbbd1a5671` tested an untried high-download legacy YOLOv5 football detector through the repaired actual-image detector-audit path. The local helper preflight correctly stopped on missing `job.write`, so the connector launched the committed raw GitHub `train.py`. The job fetched only annotations, manifest, and `val.zip`, then failed before official SSKit evaluation when Ultralytics tried to run the old YOLOv5 checkpoint and raised `TypeError: BaseModel.fuse() got an unexpected keyword argument 'verbose'`. This is a runtime-compatibility blocker, not a model score. Do not add a YOLOv5-specific loader unless there is a stronger source-specific reason than another generic public football detector. |
| 2026-05-07 | keypoint-actual-image-scale-smoke | HF Jobs connector `t4-small` | `TRAIN_MODE=keypoint SYNLOC_COORD_SCALE_MODE=actual_image YOLO_POSE_MODEL=yolo11n-pose.pt TRAIN_MAX_IMAGES=64 VAL_MAX_IMAGES=32 YOLO_EPOCHS=1 YOLO_BATCH=4 YOLO_IMGSZ=960` | 0.0000764555 | 0.198693 | keep-mechanics / discard-result | Job `69fbd6c3aff1cd33e8f2ebb6` first added and then scored actual-image coordinate scaling in the YOLO keypoint lane from committed ref `b34c4f3931b44702244d335867a85d0b246c7a42`. The run trained/evaluated on actual 1920x1080 cached images while mapping keypoint boxes and points back to 3840x2160 annotation coordinates for SSKit. Official `mAP-LocSim=7.645552200007645e-05`, above the old strict-scale `keypoint-yolo11n-smoke` `5.743033700121752e-07` but still below pose smoke `0.000825082508250825`; diagnostics remained weak: `gt_recall_iou_0_5=0.0038022813688212928`, `gt_recall_px_50=0.03802281368821293`, and mean best GT-to-pred point error about `499.6` px from 800 predictions for 526 GT boxes. Keep the keypoint scale adapter and verify guard, but discard this exact YOLO11n-pose setup as the next scoring direction. Local helper preflight correctly stopped on missing `job.write`; connector launch completed. Artifact upload failed after `AUTONOMY_RESULT` with HF model-repo LFS `403`. |

| 2026-05-07 | tmoklc-football-candidate-audit | HF Jobs connector `t4-small` | `TRAIN_MODE=baseline SYNLOC_COORD_SCALE_MODE=actual_image YOLO_BASELINES=tmoklc-football-player-detection\|tmoklc/football-player-detection\|football-player-detection.pt\|0,1,2,3 TRAIN_MAX_IMAGES=16 YOLO_IMGSZ=1280 YOLO_CONF=0.01` | 0.0073516535 | 0.465785 | keep-signal | Job `69fbdb30317220dbbd1a56a4` evaluated an untried plain Ultralytics football detector through the repaired actual-image detector-audit path from committed ref `23d8c565285c2c2241dd64411e447d57aa8fdd3f`. The model labels loaded as `0=ball`, `1=goalkeeper`, `2=player`, `3=referee`; 429 detections for 301 GT boxes gave official `mAP-LocSim=0.007351653532700209`, above pose smoke and prior direct-point synthetic smokes. More importantly, candidate diagnostics were the first useful real-source signal: `gt_recall_iou_0_5=0.840531561461794`, `gt_recall_iou_0_3=0.9269102990033222`, `mean_best_iou_gt_to_det=0.6984613095305611`, and `det_precision_iou_0_5=0.6713286713286714`. Keep this as the next real candidate source; the next loop should pair `tmoklc/football-player-detection` with `TRAIN_MODE=point_regressor` on a bounded 64/32 smoke before scaling. Artifact upload failed after `AUTONOMY_RESULT` with HF model-repo PR/write `403`; the printed log is the durable score. |
| 2026-05-07 | point-regressor-tmoklc-yolo-candidates | HF Jobs connector `t4-small` | `TRAIN_MODE=point_regressor SYNLOC_COORD_SCALE_MODE=actual_image POINT_CANDIDATE_MODE=yolo POINT_DETECTOR_BASELINE=tmoklc-football-player-detection\|tmoklc/football-player-detection\|football-player-detection.pt\|1,2,3 TRAIN_MAX_IMAGES=64 VAL_MAX_IMAGES=32 POINT_EPOCHS=2 POINT_BATCH=16` | 0.0000000000 | 0.460407 | discard-result / bridge-audit | Job `69fbdd96aff1cd33e8f2ec00` used the Hugging Face Jobs connector after local helper preflight correctly stopped on missing `job.write`. It trained the tiny direct point regressor for 2 epochs and reached official SSKit eval, but the detector-to-point bridge collapsed: official `mAP-LocSim=0.0`, `precision_50=0.0`, `recall_50=0.0`, 512 predictions for 526 GT boxes, `gt_recall_iou_0_5=0.0019011406844106464`, `gt_recall_iou_0_3=0.011406844106463879`, `mean_best_iou_gt_to_det=0.0074524783749109385`, `gt_recall_px_50=0.034220532319391636`, and mean best GT-to-pred point error about `481.3` px. This conflicts with the strong 16-frame tmoklc detector-only audit and is likely a bridge-setting mismatch first: this run used the point-regressor detector default `imgsz=960` plus top-25, while the good audit used `YOLO_IMGSZ=1280`. Rerun once with `POINT_DETECTOR_IMGSZ=1280` and a less tight candidate cap before discarding tmoklc. Artifact upload failed after `AUTONOMY_RESULT` with HF model-repo LFS `403`; the printed log is the durable score. |
| 2026-05-07 | tmoklc-matched-bridge-libgl-blocker | HF Jobs connector `t4-small` | `TRAIN_MODE=point_regressor SYNLOC_COORD_SCALE_MODE=actual_image POINT_CANDIDATE_MODE=yolo POINT_DETECTOR_BASELINE=tmoklc-football-player-detection\|tmoklc/football-player-detection\|football-player-detection.pt\|1,2,3 POINT_DETECTOR_IMGSZ=1280 POINT_MAX_DETECTIONS_PER_IMAGE=50 TRAIN_MAX_IMAGES=64 VAL_MAX_IMAGES=32 POINT_EPOCHS=2 POINT_BATCH=16` | 0.0000000000 | 0.000000 | integration-blocked | Job `69fbe081aff1cd33e8f2ec29` attempted the exact matched-settings rerun requested by the prior bridge audit. Repo-local preflight again stopped correctly on authenticated HF token missing `job.write`, so the Hugging Face Jobs connector launched committed raw `train.py` from ref `6d5881258224855734aab8923dd0f2f23b568d1a`. The job failed before training or official SSKit evaluation while importing Ultralytics/OpenCV: `ImportError: libGL.so.1: cannot open shared object file`. No model score or tmoklc bridge verdict exists from this pass. Next loop should fix the UV/OpenCV packaging surface so point-regressor jobs install/import headless `cv2` without a system GL dependency, then rerun this same matched bridge command. |
| 2026-05-07 | opencv-headless-rfdetr-lazy-install | Local helper fix | `python3 -m py_compile train.py`; `scripts/verify.sh` | 0.0000000000 | 0.000000 | keep-mechanics | Fixed the packaging surface exposed by job `69fbe081aff1cd33e8f2ec29`. The default UV payload now pins `opencv-python-headless>=4.10,<5` for Ultralytics/YOLO jobs and no longer installs `rfdetr==1.2.1` in every run, avoiding a transitive GUI OpenCV dependency before point-regressor code can execute. RF-DETR support is preserved behind lazy `ensure_rfdetr_runtime()` installation only when the RF-DETR mode is selected. `scripts/verify.sh` now guards this split so future YOLO/point-regressor jobs do not regress into requiring system `libGL.so.1`. Next loop should rerun the exact matched tmoklc bridge command from the blocker row. |
| 2026-05-07 | tmoklc-matched-bridge-scored | HF Jobs connector `t4-small` | `TRAIN_MODE=point_regressor SYNLOC_COORD_SCALE_MODE=actual_image POINT_CANDIDATE_MODE=yolo POINT_DETECTOR_BASELINE=tmoklc-football-player-detection\|tmoklc/football-player-detection\|football-player-detection.pt\|1,2,3 POINT_DETECTOR_IMGSZ=1280 POINT_MAX_DETECTIONS_PER_IMAGE=50 TRAIN_MAX_IMAGES=64 VAL_MAX_IMAGES=32 POINT_EPOCHS=2 POINT_BATCH=16` | 0.0000000000 | 0.464691 | discard-result | Job `69fbe2c0aff1cd33e8f2ec42` reran the exact matched tmoklc detector-to-point bridge after the headless OpenCV fix. Repo-local preflight correctly stopped on missing local `job.write`, so the connector launched committed raw `train.py` from ref `96ccb7eae8da508e1ccda25ca395a3bd2735fcfc`. The job reached official SSKit eval and printed `AUTONOMY_RESULT`, so the prior `libGL.so.1` blocker is resolved. Result stayed collapsed: official `mAP-LocSim=0.0`, `precision_50=0.0`, `recall_50=0.0`, 617 predictions for 526 GT boxes, `gt_recall_iou_0_5=0.0019011406844106464`, `gt_recall_iou_0_3=0.011406844106463879`, `mean_best_iou_gt_to_det=0.007551797826536668`, `gt_recall_px_50=0.04182509505703422`, and mean GT-to-pred point error about `457.3` px. The 16-frame detector-only tmoklc audit remains a useful anomaly/source signal, but this exact 64/32 point-regressor bridge is discarded; next work should audit the detector-only vs bridge validation-slice/ingestion mismatch or move to a stronger official-runtime candidate source, not rerun this command unchanged. Artifact upload failed after `AUTONOMY_RESULT` with HF model-repo LFS `403`; the printed log is the durable score. |
| 2026-05-07 | tmoklc-detector-class-slice-audit | HF Jobs connector `t4-small` | `TRAIN_MODE=detector_class_audit SYNLOC_COORD_SCALE_MODE=actual_image AUDIT_MAX_IMAGES_LIST=16,32 AUDIT_CLASS_VARIANTS=all=0,1,2,3;athletes=1,2,3;player=2 YOLO_IMGSZ=1280 YOLO_CONF=0.01` | 0.0073516535 | 0.465785 | keep-signal / bridge-bug | Job `69fbe74baff1cd33e8f2ec7c` added and scored a no-training class/slice audit from committed ref `9e75f47d5e015a284f9e0a933e7048d636aee8be` after repo-local preflight correctly stopped on missing `job.write`. Best remained the known 16-frame all-class detector-only row, but the important new result is that the detector stayed strong on the exact 32-frame bridge slice: athlete-only classes `1,2,3` scored official `mAP-LocSim=0.006958124383866958`, `gt_recall_iou_0_5=0.8384030418250951`, `gt_recall_iou_0_3=0.9106463878326996`, `det_precision_iou_0_5=0.6752368064952639`, 739 detections for 526 GT boxes; player-only still had `gt_recall_iou_0_5=0.6901140684410646` and `mAP-LocSim=0.006993505802193122`. This rules out class ids, validation slice size, actual-image scaling, and raw detector recall as explanations for the point-regressor bridge collapse; next pass should audit/fix the bridge ingestion path before the point head/top-k, not rerun the same bridge. First connector launch `69fbe651aff1cd33e8f2ec6f` failed before repo code because the raw GitHub URL used a mistyped SHA and returned `404: Not Found`. Artifact upload failed after `AUTONOMY_RESULT` with HF model-repo `403`; the printed log is durable. |
| 2026-05-07 | tmoklc-bridge-split-leak-audit | HF Jobs connector `t4-small` + local fix | `TRAIN_MODE=point_regressor SYNLOC_COORD_SCALE_MODE=actual_image POINT_CANDIDATE_MODE=yolo POINT_DETECTOR_BASELINE=tmoklc-football-player-detection\|tmoklc/football-player-detection\|football-player-detection.pt\|1,2,3 POINT_DETECTOR_IMGSZ=1280 POINT_MAX_DETECTIONS_PER_IMAGE=50 TRAIN_MAX_IMAGES=64 VAL_MAX_IMAGES=32 POINT_EPOCHS=2 POINT_BATCH=16` | 0.0000000000 | 0.464691 | blocker-fix / rerun-needed | Job `69fbea40aff1cd33e8f2eca0` ran from committed ref `e419c94b066d17b41082a958ebb803a89415c150` after adding raw/pre-topk bridge diagnostics. Official score was still `mAP-LocSim=0.0`, but `raw_detector_boxes_before_point` already matched the collapsed final box diagnostics: `gt_recall_iou_0_5=0.0019011406844106464`, `gt_recall_iou_0_3=0.011406844106463879`, 617 detector boxes for 526 GT boxes. This localizes the bridge mismatch before point crops/top-k and exposed split-image leakage: point-regressor mode downloads train+val, and duplicate basenames could resolve to train images for validation annotations. Patched `train.py` to pass split hints through image lookup and added same-basename train/val coverage to `scripts/verify.sh`. Rerun this same bridge after the split-aware commit before judging tmoklc. Artifact upload failed after `AUTONOMY_RESULT` with HF model-repo LFS `403`; the printed log is durable. |

## External Evidence Pulled In

- Sibling `../v2d-research` ran a copied SoccerMaster GSR
  adapter on 64 SynLoc frames and got `mAP-LocSim=0.0` with no decoded `player`
  detections. Treat that as integration/config evidence, not a SoccerMaster
  model verdict.
- SoccerMaster official role mapping is `ball=0`, `goalkeeper=1`, `other=2`,
  `player=3`, `referee=4`, `None=5`.
